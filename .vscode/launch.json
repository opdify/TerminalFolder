{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Terminal Projects Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/dist/**/*.js"
      ],
      "preLaunchTask": "Terminal Projects: Build"
    }
  ]
}
